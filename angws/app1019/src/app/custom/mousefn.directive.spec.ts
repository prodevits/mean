import { MousefnDirective } from './mousefn.directive';

describe('MousefnDirective', () => {
  it('should create an instance', () => {
    const directive = new MousefnDirective();
    expect(directive).toBeTruthy();
  });
});
